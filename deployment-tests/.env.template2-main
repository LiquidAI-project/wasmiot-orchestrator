# The IP address of the server running the orchestrator
MAIN_IP=

# MongoDB connection parameters
MONGO_HOST=${MAIN_IP}
MONGO_PORT=33221  # It is recommended to use a non-default port
MONGO_ROOT_USERNAME=testusername
MONGO_ROOT_PASSWORD=testpassword

# Public base URL for the orchestrator application.
ORCHESTRATOR_PUBLIC_HOST=http://${MAIN_IP}
ORCHESTRATOR_PUBLIC_PORT=8000

# MongoDB Express parameters
MONGO_EXPRESS_PORT=8081
MONGO_EXPRESS_ENABLE_ADMIN=false
MONGO_EXPRESS_BASICAUTH=false
MONGO_EXPRESS_USERNAME=test
MONGO_EXPRESS_PASSWORD=test

# Path to the folder where the initial configuration files are stored.
WASMIOT_INIT_FOLDER=$(pwd)/test2/orchestrator/init

# Whether to clear the supervisor logs at startup.
WASMIOT_CLEAR_LOGS=true

# Set to true if you want to use a WebSocket for to provide real-time logging to clients.
WASMIOT_USE_WEB_SOCKETS=true

# Paths for the configuration files
INSTANCE_PATH_PYTHON="$(pwd)/test2/device-main-python"
INSTANCE_PATH_RUST="$(pwd)/test2/device-main-rust/instance"

# Supervisor names
SUPERVISOR_PYTHON_LOCAL_NAME=-main-python
SUPERVISOR_RUST_LOCAL_NAME=main-rust

# Supervisor ports
SUPERVISOR_PYTHON_LOCAL_PORT=7001
SUPERVISOR_RUST_LOCAL_PORT=7002

# Device architecture for Rust cross compilation
DEVICE_ARCH=armv7-unknown-linux-gnueabihf
